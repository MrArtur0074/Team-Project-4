import React from "react";

const Header = () => {
    return (
        <header>
            <h1>SkillBridge</h1>
            <nav>
                <a href="/">Home</a>
                <a href="/dashboard">Dashboard</a>
            </nav>
        </header>
    );
};

export default Header;
